#!/usr/bin/env node
// PreToolUse:Write|Edit|MultiEdit — DISABLED no-op stub.
//
// This was a template hook that used the Claude Agent SDK to detect duplicate database
// query functions under a `src/queries` directory. That directory does not exist in this
// monorepo and `@anthropic-ai/claude-agent-sdk` is not a dependency here, so the original
// crashed on import. It is kept wired but inert.
//
// To re-enable: install @anthropic-ai/claude-agent-sdk, restore the analysis logic from
// git history (.claude/hooks/query-hook.js), and point it at a real queries directory.

process.exit(0);
